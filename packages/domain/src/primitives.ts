/**
 * Shared primitive aliases for domain entities.
 * Declared here to avoid circular imports between feature modules.
 */
export type EntityId = string;
export type Slug = string;
export type ISODateString = string;
